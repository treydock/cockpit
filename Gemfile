source :rubygems

group :development do
  gem 'rake'
  gem 'ruby-debug'

  gem 'test-unit'
  gem 'shoulda'

  gem 'activerecord'
  gem 'sqlite3-ruby'
end

# dependencies
gem 'defined-by'

# TODO: use `gemspec` instead
# in order to this we have to gemspec first!
